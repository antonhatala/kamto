; Kamto — PHP-FPM tweaks on top of php:8.5-fpm defaults.
date.timezone = Europe/Prague

; Don't advertise PHP in the X-Powered-By header (Nette's own header is disabled
; in config/config.neon — http.headers).
expose_php = Off
