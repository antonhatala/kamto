parameters:
	# Copy this file to config.local.neon and put your own password hash here.
	# Generate one with (v Dockeru, bez PHP na hostu):
	#   docker compose run --rm php php -r "echo password_hash('your-password', PASSWORD_DEFAULT), PHP_EOL;"
	appPasswordHash: '$2y$12$hiVouMjbTynUn8yaOo79quGx2s2zUETxIHIF1lj5uujKLarWzK8SC'

http:
	headers:
		# Vypni produkční CSP (config.neon) v lokálním dev režimu — Tracy debug bar injektuje
		# inline <script>+eval, které by striktní CSP zablokovala. null = hlavička se neposílá.
		Content-Security-Policy: null
